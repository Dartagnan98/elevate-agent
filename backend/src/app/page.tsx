export default function Page() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "3rem" }}>
      <h1>Elevate Backend</h1>
      <p>API-only. Routes at <code>/api/*</code>.</p>
    </main>
  );
}
