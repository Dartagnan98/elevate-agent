# deal-onboarding lessons

<!-- [date] | what happened | rule/insight -->
[2026-06-22] | Skill authored from spec. | Source of truth for every field = knowledge/deals/card-field-sources.md; read it before populating any field, don't improvise a source.
[2026-06-22] | Never guess a price/date/legal/PID. | If the signed doc (CPS/MLC/SR/Title) isn't available, leave the field BLANK and add a card reminder naming the doc to pull. A fabricated value is a defect; a blank + flag is correct.
[2026-06-22] | Infer stage from evidence, don't assume Stage 0. | A user onboarding mid-pipeline has live listings and firm deals. MLS# present ⇒ listing ≥ Listing Live; signed CPS ⇒ ≥ Accepted Offer; signed final SR ⇒ ≥ Subject Removal. Absence of a higher-stage artifact is itself a signal — don't fill a field to force a stage.
[2026-06-22] | Toggles drive required legal docs. | Never guess a toggle. Ask the Stage-1 branching question; if unanswerable, set the conservative default (No / None / Individual / Standard) and flag, because a wrong toggle silently hides a required doc.
[2026-06-22] | GCI was being logged as the TOTAL commission | GCI = ONLY the commission the realtor keeps, and only after an accepted offer. Listing side = total − cooperating split (from the signed MLC). Buyer side = the cooperating commission on the MLS of the property bought. commission_pct stays the total listing rate (not the GCI). Never assume the split — read it from the MLC/MLS.
[2026-06-22] | Skill hardcoded one realtor's sources (a specific sheet ID, board, province default, CRM key, Drive folder name) — unsafe for a brand-new tenant | Load `GET /api/admin/setup` FIRST (Step 0) and resolve province/market/board, crmProvider, mlsProvider+login, complianceProvider+login, driveProvider+defaultFolderPattern, formsProvider, signingProvider, commissionNotes, regionalMemory, approvalPolicy. Every downstream step uses THESE. card-field-sources.md is one realtor's instance as a worked example, not defaults. Blank provider = skip + note, not an error.
[2026-06-22] | A shallow single-batch CRM search missed a real contact plus 5 others in the first audit | CRM matching must be FULL pagination + cross-check. Lofty: GET https://api.chime.me/v1.0/leads, header Authorization: token <key>, pageSize=200, paginate via data._metadata.scrollId, id field leadId; match by name AND cross-check street address / email. Other providers: equivalent full-scan + cross-check. Never stop at page one.
[2026-06-22] | Login-gated data was being left as a silent blank | Data behind a login with no API (MLS year/lot/PID via mlsProvider portal + MFA; compliance/SkySlope buyer-side money via complianceProvider; commission inside a signed MLC) must become a waiting_human card item naming the exact source — never an unflagged blank.
[2026-06-22] | First end-to-end DRY-RUN test on a real realtor's own pipeline (23 active deals) passed: tenant config read (province/board/CRM/MLS/compliance/Drive), all deals discovered, stages inferred correctly across a mix of listing and buyer deals at different stages, GCI only on accepted-offer deals (kept-share), gaps surfaced as waiting_human not silent (missing year/lot+contact, buyer-side contact+SkySlope GCI, a test-property contact). | Validated. NOTE: unauthenticated GET /api/admin/setup returns an empty profile — run with harness auth (or read admin_setup_profile) for the real tenant config.
[2026-06-22] | SkySlope missing-docs must be part of onboarding too, not just the Mon/Wed cron | deal-onboarding Step 4b runs skyslope-sync per deal and writes extra.skyslopeMissing + skyslopeCheckedAt (same fields the card section reads + the audit writer writes). Never write an empty array when the portal was unreachable — flag waiting_human.
[2026-06-23] | Card cleanup: ticking completed-stage checklist items must be part of onboarding, not a one-off | When setting a deal stage, back-fill extra.checklistManual with every checklist itemKey ($phaseId:$idx:$item) for phases whose stage number < current_stage (union-merge). Completed stages only; current/future stay on live auto-conditions. Added as mechanism #2 in the Step 4 checklist note.
[2026-06-23] | The audit missed a card that existed in the source systems but not on the board | The audit MUST reconcile each source (SkySlope, Drive/compliance folders, MLS active listings, listings sheet, signed contracts) against the existing cards and CREATE a card for anything found there but missing — not just hydrate existing cards. A real active listing (Drive folder + active MLS) sat off the board because the pass only hydrated existing deals. BUT: active/in-flight only — a closed prior-year file is NOT a missing card (a listing that closed the prior year → correctly excluded). Check status before flagging. (Step 1b.)
[2026-06-24] | "Wiped" card fields root cause: writers (card UI human:web, the admin_deal agent, one-off *-sync runs) saved property fields under the NAMESPACED UI key (core.pid, core.city, extra.checklistManual) but the card display reads the BARE key (pid, city, checklistManual). set_deal_toggle stores the field name verbatim, so prefixed writes land where the card cannot read them and the value is invisible — looked deleted but was never lost. | ALWAYS write extra-toggle fields under the BARE key the card reads — NEVER core.X / extra.X. The card UI ids are namespaced but that prefix is UI-only. See card-field-sources.md "Storage-key convention". Backend set_deal_toggle now strips a leading core./extra. as a safety net (ships in the app), but write bare anyway so it is correct on tenants whose app has not updated. Restored a realtor's 11 hidden fields via jsonb migration + added the backend guard 2026-06-24.
