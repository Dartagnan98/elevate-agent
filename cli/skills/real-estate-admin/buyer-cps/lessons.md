
[2026-06-28] | Offer-kit forms multi-form build | One GENERIC filler (knowledge/deals/forms/fill-form-generic.py) fills ANY WEBForms fillable PDF from a shared deal context by recognizing the standard field-name convention (txtbuyer1, txtp_street, txtCurrentUserFullName, txtp_closedate_mmmm...). Adding a form = drop its blank fillable template in knowledge/deals/forms/<id>-template.pdf + add to TEMPLATES dict in admin_deals generate endpoint + the build docs list. WEBForms exports are FLAT unless you hit the "download fillable" button. Subject Removal AoIR blank is flat (0 fields) — can't auto-fill. Engine runs via /usr/bin/python3 with clean env {PATH,HOME} (app PYTHON* vars break user-site pypdf lookup).
