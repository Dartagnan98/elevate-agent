# Elevate 1.2.44 — release notes

## Polish: the steer bar matches the composer bars

The "While it's working, send as" strip now renders as a proper bar — same
card styling as the workspace bar above it — instead of floating text
between the two.
