# Elevate UI production polish plan

Started 2026-05-13. Goal: ship-ready realtor UI through small, verifiable slices.
Working out of `cli/web/src/`. Build verified after every slice (`npx tsc -b && npm run build`).

## Method

- Touch one surface per slice. tsc + vite must both stay green.
- Stage by file name, never `git add -A`.
- Each slice ends in a local commit, message scoped to the surface.
- ChatPage.tsx and PageHeaderProvider.tsx are dirty user work — do not touch.
- Audit findings are recorded here; "DONE" lines below mark verified ship.

## Already shipped this session (pre-goal context)

- `/today` redesigned (pulse strip, priority queue, day shape, running strip).
- `/today` lazy-loaded into its own chunk.
- Draft rows on `/leads` clean up RFC822 envelopes; provenance deduped; terracotta stripe killed.
- Lane sections on `/leads` compacted (title + count only, empty-state collapses to one line).

## Audit by page

### P0 — must fix before this counts as production

1. **Tasks page** lists six surfaces (handoffs, worker, deal tasks, action runs, timed tasks, sessions×2) but **no approval queue**.
   The page name promises action; the content is a status dump. → Restructure around "what needs me right now": waiting-human handoffs and pending deal tasks become the headline, everything else moves below.
2. **Agent Hub** handoff card shows counts + worker state but the "next action" is buried.
   When the worker is unhealthy the user shouldn't have to read six chips to find the fix. → Surface the single most-important call to action as a primary affordance.
3. **Outreach lanes vs Automations vs Tasks** — same operational concept (timed jobs) rendered three different ways. → Make the relationship visible: the lanes block on `/leads` already lists the cron jobs, but the styling makes it look like a different system. Reuse the same row UI, label both as "Automations" sub-views. **DONE in S11** (5a932e8).
4. **Cron page row width**: schedule, last, next all crammed on one line; with long names the row overflows on tablet. → Stack schedule on its own line under the title on `<lg`.

### P1 — visible polish

5. **AgentHubPage AgentCard** — `MiniMetric` shows "Sessions / Active" with no context; for a realtor this is meaningless. Either retitle to "Last 24h" / "Live now" or drop and show last run timestamp.
6. **AgentHubPage Telegram lane card** — state strings ("Missing bot token", "Duplicate bot token") are correct but the action ("paste from BotFather here") isn't obvious. The Save button doesn't enable until both fields have values; the form should say so.
7. **Memory page WorkflowStrip** — six counters of backend table sizes (facts, entities, documents, chunks, communities, relations) — none of these help a realtor decide anything. → Replace strip with two meaningful tiles (pipeline health + most-recently-ingested session) and drop the rest.
8. **Social media page** — `summaryStats` four tiles always render even when zero. Heavy `py-12` empty state in the idea queue. → Collapse summary to one line until there is data, tighten the empty state.
9. **Memory page graph card** — `min-h-[38rem]` even when there are zero nodes. → Set a max-height fallback and an inline empty state for the empty graph.
10. **Sidebar Automations section** — uses its own custom collapse button while the other section labels use the shared `SidebarSectionLabel`. → Unify to one component so visual rhythm holds.

### P2 — quality cleanup

11. **Tasks WorkflowStrip** repeats "Task errors" and "Memory queue" which appear nowhere else on the page — they belong on Memory and Cron respectively.
12. **Today page** uses `Home` icon. Hub title is "Elevate Agent · Today" — drop the "Elevate Agent" prefix (every page already lives inside Elevate; redundant).
13. **HubShell hero copy** on Tasks, Memory, Social — long marketing-style sentences. Strip or shorten to 1 short clause.
14. **Cron form** — "New job" card is full-width above the list. Most users come to this page to look at existing jobs. → Collapse create form behind a button until they need it.
15. **Social media `refresh from platforms` button** — uppercase mono, 44px min height. Way too loud. → Standard small button.

## Implementation slices

| # | Slice | Files | Verify |
|---|---|---|---|
| S1 | **Lane cleanup baseline commit** (already done in editor; commit it) | `_shared/index.ts`, `_shared/parse-identity.ts`, `_shared/use-hub-data.tsx`, `today/*`, `RealEstateHubPages.tsx`, `App.tsx` (today lazy) | tsc + vite |
| S2 | **Tasks page restructure** (P0 #1, P2 #11, #13) — reorder around what's waiting on the user. | `real-estate-hub/tasks/index.tsx` | tsc + vite |
| S3 | **Agent Hub handoff card** (P0 #2, P1 #5, #6) — surface single primary action; tighten metrics; clarify Telegram form. | `AgentHubPage.tsx` | tsc + vite |
| S4 | **Memory page WorkflowStrip rewrite** (P1 #7, #9, P2 #13) | `real-estate-hub/memory/index.tsx` | tsc + vite |
| S5 | **Social page summary + idea queue empty state** (P1 #8, P2 #15) | `real-estate-hub/social/index.tsx` | tsc + vite |
| S6 | **Cron page row layout + collapse create form** (P0 #4, P2 #14) | `CronPage.tsx` | tsc + vite |
| S7 | **Sidebar unify section collapse** (P1 #10) | `App.tsx` | tsc + vite |
| S8 | **Today header trim** (P2 #12) + hub-shell hero strip | `today/index.tsx`, possibly `_shared/hub-shell.tsx` | tsc + vite |

## Change log

- **S1** 547b90d — Redesign /today around priority queue and compact /leads lanes.
- **S2** 2465f42 — Restructure /tasks around approval queue. Replace 6-surface dump (workflow strip, handoff card, worker card, deal tasks, action runs, timed tasks, two session lists) with summary tiles → Waiting-on-you board (handoffs, deal tasks, admin runs in 3 columns) → in-flight strip → scheduled + history. Tasks page now its own chunk.
- **S3** 3f11a42 — Agent Hub: HandoffBusCard picks its primary CTA from worker health (Wake loop when stopped, Run worker now when work is queued). AgentCard MiniMetric pair replaced with single contextual "X active now" line. Telegram lane gets a one-liner explaining both fields required and where to find the bot token. Drop "Memory queue" metric (belongs on Memory).
- **S4** db8038a — Memory page: drop 6-tile WorkflowStrip (Facts/Entities/Documents/Chunks/Communities/Relations) for three operator-meaningful tiles (pipeline state · last ingest · embeddings model). Bound graph height max-h-[38rem] min-h-[24rem] and show inline empty state when nodes is empty. Drop marketing hero copy.
- **S5** 9d09173 — Social page: zero-state collapses to a single muted line instead of always-four-tile WorkflowStrip. Idea queue + posts empty states use single-line muted text matching /leads behavior. Refresh, Show 100 more, Show all, and lookback select drop the loud `font-mono-ui uppercase tracking-wider min-h-[44px]` styling.
- **S6** 2018c5f — Cron page: collapse New job form behind a button at the list header (auto-closes after a successful create). Job row meta wraps cleanly with schedule on its own line under lg.
- **S7** ffe2713 — Sidebar: AutomationsSection now uses SidebarSectionLabel for its toggle, matching every other section heading. Restores vertical rhythm.
- **S8** 97e8cb0 — Today + Admin: drop "Elevate Agent" prefix and marketing-style hero/title copy. Today = "Today", Admin = "Admin".
- **S9** 09e0733 — Skills page: drop "Skills live here; dashboards show connectors." marketing tagline; collapse workflow section header to single muted eyebrow + counter badge. Three empty states (search, category, toolset grid) trade py-8 text-center for the muted single-line style used on /leads.
- **S10** 1d50ba8 — Settings + OAuth: remaining heavy empty states (ConfigPage search results, OAuthProvidersCard zero-providers) drop py-8/py-12 text-center for the muted single-line style.
- **S11** 5a932e8 — Outreach lanes: P0 #3 fix. AgentLaneStripRow rebuilt around the cron-page row layout (inline icon + title + shadcn Badge mapped from laneStatus, muted tagline, schedule/last/next as plain text, icon-only Pause/Run/Edit). Edit deep-links to /cron?edit=id so the lane and the underlying job are visibly the same record. Section labels reframed to "Outreach automations" and "Other automations".

## Overnight audit table (started 2026-05-13)

Format per `cli/docs/ui-production-polish-overnight-goal.md`. P0=broken, P1=confusing/duplicated, P2=spacing/labels, P3=polish.

| Status | Severity | Route | Component | Issue | Fix | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| done | P1 | `/agent-hub` | `AgentHubPage.tsx` HarnessCard | 7 MiniMetric tiles (Clients/Routed/Events/Ready Runs/Blocked/Safety/Memory Flow) are technical noise; realtor cannot act on these | Surfaced 3 operator-facing tiles (Ready / Blocked / Safety) in a clean 3-col grid; tucked Clients/Routed/Events/Memory flow + lifecycle states behind a `<details>` "Show technical detail" disclosure | tsc + vite clean (S18) |
| done | P2 | `/memory` | `real-estate-hub/memory/index.tsx` right Pipeline card | 4 HubMetric (Pending/Processed/Failed/Active sessions) duplicate the SummaryTile pipeline state; first 3 are the same data shown twice | Repurposed right card as "Recent ingest"; replaced 4-tile grid with single "Active sessions" row + provider strip + recent sessions list | tsc + vite clean (S16) |
| done | P2 | `/social` | `real-estate-hub/social/index.tsx` "No snapshot yet" Card | `py-6 text-center` heavy empty state, matches the pattern already cleaned across leads/skills/oauth | Replaced with single-line muted text in `text-xs text-muted-foreground/80 px-1 py-1` style | tsc + vite clean (S14) |
| done | P2 | `/social` | `real-estate-hub/social/index.tsx` idea queue loading | `flex items-center justify-center py-12` loading block jumps layout | Switched to a muted single-line loading row | tsc + vite clean (S14) |
| done | P2 | `/social` | `real-estate-hub/social/index.tsx` "Your posts" + "All posts" headers | Subtitles use loud `font-mono-ui uppercase tracking-wider` even after S5 cleanup | Dropped uppercase mono on subtitles + "{N} of {M}" counter; normal sentence case in muted text | tsc + vite clean (S14) |
| done | P2 | `/cron` | `CronPage.tsx` empty state | `py-8 text-center` heavy empty state pattern already cleaned elsewhere | Switched to muted single-line text matching the leads/skills convention | tsc + vite clean (S15) |
| done | P2 | `/cron` | `CronPage.tsx` loading state | `py-24` causes large layout jump while jobs load | Reduced to muted single-line loading text | tsc + vite clean (S15) |
| done | P2 | `/tasks` | `real-estate-hub/tasks/index.tsx` DealTaskRow "Open deal" link | `font-mono-ui text-[0.68rem] font-semibold uppercase tracking-[0.12em]` link inconsistent with "Run AI" Button next to it | Converted to `<Link>` styled with `cn(buttonVariants({ size: "sm", variant: "ghost" }))` matching the shadcn Button used for "Run AI" | tsc + vite clean (S17) |
| done | P1 | `/tasks` | `real-estate-hub/tasks/index.tsx` HandoffRow | Shows raw `fromAgentId → toAgentId` enum slugs with no human labels | Added `humanizeAgentId` helper; HandoffRow now renders title-cased "From → To" labels. Action button deferred — needs handoff-resume API endpoint first | tsc + vite clean (S17) |
| done | P1 | `/sessions` | `SessionsPage.tsx` SessionRow + ToolCallBlock | `border-l-2 border-l-warning` (ToolCallBlock) and `border-l-2 border-l-success` (active SessionRow) are banned border-left stripes | Dropped both stripes. Active SessionRow already identified by pulsing "Live" badge; ToolCallBlock keeps warning text colour with a clean border | tsc + vite clean (S13) |
| done | P2 | `/sessions` | `SessionsPage.tsx` expanded session content | `py-8` loading, `py-4 text-center` empty/error inside the row | Switched to three single-line muted-text variants | tsc + vite clean (S13) |
| done | P2 | `/logs` | `LogsPage.tsx` error block | `border-l-2 border-l-destructive` stripe vs no-stripes design rule | Dropped left stripe; destructive text + bg-card row + bottom border | tsc + vite clean (S12) |
| done | P2 | `/logs` | `LogsPage.tsx` empty log lines | `text-muted-foreground text-center py-8` heavy empty state | Switched to single-line muted text | tsc + vite clean (S12) |
| done | P2 | `/analytics` | `AnalyticsPage.tsx` loading/error/empty | `py-24` loading, `py-6 text-center` error, `py-12` empty with centered icon | Single-line muted text for all three; dropped large centered BarChart3 icon | tsc + vite clean (S12) |
| done | P3 | `/project` | `ProjectPage.tsx` loading | `py-24` initial loading block jumps layout | Single-line muted loading row; removed now-unused Loader2 import | tsc + vite clean (S13) |
| done | P3 | `/env` | `EnvPage.tsx` loading | `py-24` initial loading block jumps layout | Single-line muted loading row | tsc + vite clean (S13) |
| done | P2 | `/setup` | `DesktopSetupPage.tsx` loading state | `flex items-center justify-center py-24` + Loader2 animate-spin — heavy loading pattern already cleaned across other pages | Replaced with single-line `text-xs text-muted-foreground/80` muted text; removed Loader2 import | tsc + vite clean (S19) |
| done | P2 | `/setup` | `DesktopSetupPage.tsx` pack-selection buttons | `border-border border-l-2 border-l-primary bg-muted` active state — banned border-left stripe pattern | Dropped stripe; active state now `border-primary bg-muted text-foreground` (primary-tinted border for clear affordance) | tsc + vite clean (S19) |
| done | P2 | `/setup` | `DesktopSetupPage.tsx` step tabs (Unlock/Providers/Credentials) | Same `border-border border-l-2 border-l-primary bg-muted` active state on the 3 step tabs | Dropped stripe; matched the pack-button treatment with `border-primary bg-muted text-foreground` | tsc + vite clean (S19) |
| done | P2 | `/settings` | `ConfigPage.tsx` 5 status/error panels | Custom-auth error, preset notice, test-result success/warning ternaries (CRM preset + custom), plugin discovery error — all use `border-l-2 border-l-{destructive,warning,success}` banned stripes | Dropped all 5 stripes; status now communicated by destructive/warning/success text colour on uniform `border-border bg-card` row | tsc + vite clean (S20) |
| done | P3 | `/settings` | `ConfigPage.tsx` main loading + plugin loading + YAML editor loading | `py-24` initial spinner, `py-8` plugin spinner, `py-12` YAML spinner — heavy loading blocks | Switched all three to muted single-line text matching the convention | tsc + vite clean (S20) |
| done | P3 | `/settings` | `ConfigPage.tsx` plugin empty state | `border border-border/50 px-4 py-6 text-center` heavy empty state | Single-line muted text; preserved the code-formatted plugins path | tsc + vite clean (S20) |
| done | P3 | `/settings` | `ConfigPage.tsx` plugin source Badge | `font-mono text-[0.65rem] uppercase tracking-wide` uppercase-mono pattern flagged in the systemic audit | Dropped uppercase-mono; left a clean `text-[0.7rem]` outline Badge | tsc + vite clean (S20) |
| done | P3 | `/skills` | `SkillsPage.tsx` loading state | `flex items-center justify-center py-24` heavy spinner block | Single-line muted text matching the convention | tsc + vite clean (S21) |
| done | P2 | `/today` | `real-estate-hub/today/{pulse-strip,day-shape,priority-queue}.tsx` section subtitles | 4 `font-mono-ui text-[0.62-0.66rem] uppercase tracking-[0.14em]` eyebrows sit directly under h2/h3 titles — these are subtitles, not column headers; MEMORY rule reserves mono-uppercase for structural labels (column headers, timestamps) | Switched to plain `text-xs text-muted-foreground`; mono-uppercase preserved on chart axis hour labels, KIND_LABEL row tags, waited-minutes timestamp, PulseCard stat tile labels, and run status/timestamp lines where it's actually structural | tsc + vite clean (S21) |
| done | P3 | `/today` | `priority-queue.tsx` + `running-strip.tsx` "Open leads" / "Open" links | Uppercase-mono header links — loud for a text link | Plain `text-xs text-muted-foreground hover:text-foreground` matching the rest of the site's link microcopy | tsc + vite clean (S21) |
| done | P3 | `/today` | `priority-queue.tsx` empty + `running-strip.tsx` EmptyRow | `px-3.5 py-6 text-center` and `px-3 py-4 text-center` heavy empty states | Single-line muted-text in the established `text-xs text-muted-foreground/80` convention | tsc + vite clean (S21) |
| done | P3 | `/today` | `day-shape.tsx` chart Legend | Legend chip values ("5 in", "Out") rendered in `font-mono-ui uppercase tracking-[0.12em]` — they're plain English text, not structural data | `text-[0.7rem] tabular-nums` — keeps tabular alignment without the uppercase-mono noise | tsc + vite clean (S21) |
| done | P1 | `/leads` + `/today` | `RealEstateHubPages.tsx` (systemic) | 8 `border-l-2 border-l-*` stripes scattered through the file: WaitedChip (2 ternary cases at L636/638), density toggle Compact/Expanded (L1108/1121), buyer tier HOT/WARM (L1541/1543), lane tab active state (L1884), OutreachTemplateCard warning frame (L2732), inline error panel (L2971), new-template form (L3037), blocked-source warning (L3340) | All 8 stripes removed. WaitedChip + buyer tier + inline error keep destructive/warning text colour. Density toggle + lane tab use `bg-muted text-foreground` for active. New-template form drops accent to plain `border-border bg-card`. OutreachTemplateCard + blocked-source rely on AlertTriangle icon + warning text already present | tsc + vite clean (S22) |
| done | P1 | `/admin` | `real-estate-hub/admin/index.tsx` border-left stripes (sub-slice a) | 9 `border-l-2 border-l-*` stripes — `TOP 25` warning frame (L1597), stage-list active card (L1667), inline error band (L2018), advance-prompt banner (L2616), buyer/seller side toggle active (L2985), selected-contact card (L3042), submit error (L3310), admin-setup error (L3606) | All 9 stripes removed. Status communicated through warning/destructive text colour already on each block; active-state highlights via `border-primary` (active card) and `border-primary bg-muted` (side toggle), matching the convention established in S19/S22 | tsc + vite clean (S23) |
| done | P2 | `/admin` | `real-estate-hub/admin/index.tsx` form labels + section dividers (sub-slice b1) | NewDealDialog form `<label>` elements (Side L2968, Province L2998, Contact L3038, Title L3150, Starting stage L3165, Notes L3287) + 4 admin-setup section dividers (Realtor profile L1071, Providers L1086, Credentials L1105, Workflow notes L1117) + Property/Search sub-section header (L3186) + "auto-filled from contact" inline hint (L3291) all rendered in `font-mono-ui ... uppercase tracking-wider`. These are not column headers/timestamps/code — they're form-field labels and section headings | Form labels → `mb-1.5 block text-[12px] font-medium text-muted-foreground` matching existing inline pattern. Section dividers → `mb-3 text-[12px] font-semibold text-muted-foreground`. Sub-section header → `text-[12px] font-semibold text-muted-foreground`. Hint → plain `text-[0.7rem] text-primary` | tsc + vite clean (S24) |
| done | P3 | `/admin` | `real-estate-hub/admin/index.tsx` remaining loud mono (sub-slice b2) | After (b1), 4 final misuses remained: (1) AdminKanbanCard pinned-card hand-positioned `absolute left-0 ... bg-warning` stripe at L1340 — a manual border-l warning bar duplicating the "Top" chip already shown in the card corner; (2) AdminKanbanColumn empty-state subtitle (L1518) in caps-mono looked like an active column header in an empty column; (3) "Pending approvals" eyebrow heading at L2363 — short heading, not column header; (4) "Move card →" CTA button at L2628 in mono-caps long-text | (1) removed the absolute stripe — "Top" chip already signals pinned state; (2) replaced with single-line muted text `px-3 py-3 text-xs text-muted-foreground/70`; (3) → `text-[12px] font-semibold text-warning`; (4) → `text-[0.8rem] font-medium text-primary` button text. Remaining ~45 mono uses across the file are kanban column counters, status enum chips, dialog breadcrumb meta, timestamps — legitimate per MEMORY rule | tsc + vite clean (S25) |
| done | P2 | `/admin` | `real-estate-hub/admin/index.tsx` long-text touch target | "Move card →" `min-h-11` button at L2628 used mono-caps for long-text CTA | Resolved as part of S25 (#4 above) | tsc + vite clean (S25) |
| done | P3 | `/leads` + `/today` | `RealEstateHubPages.tsx` remaining loud mono-uppercase | 4 visual misuses left after S22: (1) ContactOverviewBoard empty-state h4 "No profiles yet" (L344, `py-8 text-center`); (2) HotLeadsList "Inbox empty" h4 (L1358, `py-10 text-center`); (3) filter-bar "Filter" prefix label (L1841) — icon + word is enough without caps-mono shouting; (4) "{N} variants awaiting approval" pending-approval heading (L3021) — semantic h4, not a column header | (1)+(2) → single-line muted `text-xs text-muted-foreground/80` matching the established empty-state convention; (3) → plain `text-xs text-muted-foreground` keeping icon + word; (4) → `text-[12px] font-semibold text-warning` (same as S25 admin pending-approvals heading). Other mono-uppercase uses in the file kept — they're list-section dividers (Composio/Available channels at L372/2342/2473), status enum chips (OAuth state at L2532), keyboard popover header, and metadata rows | tsc + vite clean (S26) |
| done | P1 | cross-page | `components/{OAuthLoginModal,ToolCall,ModelPickerDialog,ChatSidebar,LoginCard}.tsx` + `pages/{RealEstateTemplatesPage,SessionsPage,real-estate-hub/social-media-widgets}.tsx` | 12 final `border-l-2 border-l-*` stripes across shared components + 3 other pages, scattered through error panels (OAuth/Login/Templates/Sessions alerts), tool-call running/error states (ToolCall.tsx — already had Zap icon + pulsing dot), active-state model-picker and provider items, ChatSidebar destructive banner, social-media-widgets filter pill + thumb highlight. With these out, the entire `cli/web/src` tree has 0 `border-l-2` usages | All 12 stripes removed. ToolCall STATUS_TONE collapsed to single `border-border bg-card` for all 3 statuses (Zap icon + pulse already communicate state). Destructive panels keep destructive text colour. Active states use `border-primary` (model pickers/SessionDetailCard/social filter pill/thumb highlight). Verified via grep: zero `border-l-2` in cli/web/src/**.{ts,tsx} | tsc + vite clean (S27) |
| done | P1 | `/agent-hub` | `AgentHubPage.tsx` HarnessCard + Access section | Three density issues: (1) HarnessCard still rendered Memory activity rows inline + a permanent "Performance profiles skipped" line when measurement was unavailable — visible noise that the user cannot act on. (2) The whole Harness block sat above Access at full height even when nothing was wrong, dominating the page below Agent handoffs. (3) Access entitlements wrapped via `flex flex-wrap gap-y-1` into a multi-row pile of pills | (1) Removed the `harness.performance.error || "Performance profiles skipped"` fallback branch entirely. (2) Wrapped the HarnessCard invocation in a closed-by-default `<details>` labeled "System status" with show/hide hints. (3) Changed Access pills container from `flex flex-wrap` to `flex items-center gap-x-3 overflow-x-auto whitespace-nowrap` with hidden scrollbars so entitlements ride one row | tsc + vite clean (S28 — 00e76cf67) |
| done | P1 | `/agent-hub` | `AgentHubPage.tsx` HandoffBusCard metric grid | HandoffBusCard headline metric row was `grid-cols-3 md:grid-cols-6` showing Queued / Running / Human + Last handoffs / Last admin / Wakes side by side. The right three are drain history (config + historical counters), not in-flight state. Below the grid, the chip row mixed live status (auto-drain, wake loop, heartbeat) with config (handoff cap, admin cap), pushing 5+ chips onto one line | Reduced the headline grid to `grid-cols-3` (Queued / Running / Human only — the three numbers an operator acts on). Dropped "handoff cap" / "admin cap" from the live chip row. Last handoffs / Last admin / Wakes / handoff cap / admin cap moved behind a closed-by-default `<details>` "Show drain history" toggle, matching the System status pattern from S28 | tsc + vite clean (S29 — aec1e6d37) |
| done | P2 | `/agent-hub` | `AgentHubPage.tsx` HandoffBusCard zero-state | After S29 the headline grid still rendered Queued 0 / Running 0 / Human 4 even with no active work — the "· N open" subtitle in the card header already conveys the same info. byAgent strip listed every agent with its waiting-human count, duplicating data already on each handoff card below. Each handoff card's from/to/timestamp meta wrapped onto two visual lines via flex-wrap with separate spans | Wrapped the metric grid in `(queued > 0 \|\| running > 0)` so it disappears when nothing is in flight. Filtered byAgent strip to only agents with queued or running handoffs so it vanishes when everything is waiting on humans. Collapsed handoff row meta from wrapping flex to a single `truncate text-xs` line. Empty state shrunk from `py-4 text-sm` to the muted single-line convention | tsc + vite clean (S30 — c66ed6f4b) |
| done | P3 | `/agent-hub` + `/sessions` + `/leads` | `AgentHubPage.tsx`, `SessionsPage.tsx`, `real-estate-hub/thread-drawer.tsx` | Three heavy loading/empty patterns survived the prior `py-24`/`py-12` sweeps: (1) AgentHubPage loading used `flex items-center justify-center py-24` with Loader2 spinner and "Agent Hub unavailable" used `py-24 text-center text-muted-foreground`; (2) SessionsPage initial loading used the same `py-24` block with a hand-rolled spinner; (3) thread-drawer loading used `py-12` + Loader2 row and the empty state was a full `rounded-2xl border bg-card/60 px-6 py-12 text-center` card with a banned `font-semibold uppercase tracking-[0.12em]` mono "Empty thread" eyebrow. After grep, zero `py-24/py-12` heavy patterns remain in `cli/web/src/pages/**` outside of LoginCard | All three converted to the `px-1 py-1 text-xs text-muted-foreground/80` single-line muted convention used across S12/S13/S15/S19/S20/S21. thread-drawer error frame dropped `rounded-xl` to `rounded-md` to match surrounding surfaces | tsc + vite clean (S31) |
| done | P0 | `/chat` (cross-stack) | `cli/run_agent.py:11305-11316` → `cli/tui_gateway/server.py:_status_update` → `ChatPage.tsx:gw.on("status.update")` | Hard API-call failures (retries exhausted, fallback failed, "❌ API failed after N retries — Connection error" / "TimeoutError" — exactly what's hitting Codex right now) only emit via `_emit_status` → `status_callback("lifecycle", text)` → ws `status.update` event. Frontend `displayStatusText` strips the leading `❌` and routes the text through the bottom status pill + ephemeral activity-trace timeline only. There is no persistent inline message in the transcript explaining the turn died. User sees prompt sent, no streaming activity (Codex is non-streaming on gpt-5.5 anyway), pill flashes "API failed after 3 retries — Connection error" then resets to "Ready" when the run thread exits. Looks identical to "agent did nothing." Reproduced 2026-05-14 in `~/.elevate/logs/gateway.log`: 3 retry attempts × 300s no-response + 1260s connection retry = ~25 min of black-box silence before a fleeting pill flash that the user almost certainly missed. The frontend ALREADY handles a `gw.on("error")` event (ChatPage.tsx:2373) which `appendMessage("system", message, { status: "error" })` — backend just needs to fire it on retries-exhausted, not only on uncaught exceptions (server.py:2429). | Added `error_callback` param to `Agent.__init__` (run_agent.py:820) + `self.error_callback` (L1026) + `_emit_error(message)` helper next to `_emit_status` (L2279). Called from both terminal failure branches: (1) non-retryable client error at L11248-11254 — emits "API call failed (HTTP {status_code}): {summary}", appends "Run `elevate auth` to reconnect Codex." for openai-codex 401; (2) retry-exhausted at L11311-11315 — emits "API call failed after N retries: {summary}" with appended "If you're on Codex, run `elevate auth` to refresh credentials." when summary contains connection-error / timeout / network keywords. Rate-limit branch emits "Rate limited after N retries: {summary}". Wired `error_callback=lambda text: _emit("error", sid, {"message": str(text)})` in `tui_gateway/server.py:_agent_cbs` (L1119). Frontend `gw.on("error")` (ChatPage.tsx:2373) already calls `appendMessage("system", message, { status: "error" })` + `setBusy(false)` + `setQueuedInputs([])` + flips running subagents to error + statusText="Error". Net: terminal failures now appear as a persistent destructive-toned system message in the transcript that survives refresh. | tsc + vite + `python3 -c "import ast; ast.parse(...)"` clean (S32) |
| done | P2 | `/admin/templates` | `RealEstateTemplatesPage.tsx` EmptyState + Retired rationale | (1) `EmptyState` (L403-411) rendered a `min-h-[16rem] py-10` dashed-border card with a banned `font-mono text-xs uppercase tracking-wider` eyebrow on **non-structural** title copy — same pattern killed in thread-drawer (S31). Hit on all three tabs (Live/Proposed/Retired) any time the queue is empty, which on a fresh install is always. (2) Retired template rationale (L388-389) used a banned `border-l border-border/60 pl-3` left-stripe — exactly the pattern wiped out in S27 across the rest of the tree | (1) Collapsed `EmptyState` to a single muted line: `<p className="px-1 py-1 text-xs text-muted-foreground/80">{title}. {hint}</p>`. Drops the dashed border, 16rem min-height, py-10, and uppercase-mono eyebrow. (2) Retired rationale dropped `border-l border-border/60 pl-3` → plain `text-xs italic text-muted-foreground/85` | tsc + vite clean (S33) |
| done | P0 | `/chat` (cross-stack) | `cli/tui_gateway/server.py` `command.dispatch` + `cli/web/src/pages/ChatPage.tsx` resume reconciliation | Two real bugs user hit during chat: **(A)** `/restart` and `/stop` appear in slash autocomplete (declared in `commands.py:158` and `_mirror_slash_side_effects` respectively) but neither has a handler in `command.dispatch` (server.py:3304-3434, only queue/q + retry + steer were wired). Typing `/restart` or `/stop` returned `error: not a quick/plugin/skill command: restart` to the transcript. **(B)** ChatPage `hasPendingTurn(messages)` (L602) is a backward scan that returns `true` when the last non-system message is `user` with no assistant follow-up. On session resume L1899 sets `busy=true` + statusText="Resuming work..." if the cached transcript looks pending. When the server fetch confirms no pending turn (L1916), it cleared statusText="Ready" but **left busy=true**. So pill flipped to Ready but the composer + queued-input state were stuck behind a "Resuming work" mirage with no actual gateway activity to back it up. This was the user's "chats are stuck / interrupting isn't interrupting" — partly because the busy state was orphaned, partly because for previous-S32 failures the assistant message never landed in the transcript so every refresh re-triggered the heuristic | **(A)** Added `if name == "restart":` and `if name == "stop":` branches inside `command.dispatch` next to the existing queue/q/retry/steer cases. `/restart` spawns `python -m elevate_cli gateway restart` via subprocess.Popen + start_new_session=True (same mechanism as `web_server.py:/api/gateway/restart`, the button) and returns `⏳ Restarting gateway (pid N)... active runs drain first.` as an `exec` output. `/stop` calls `agent.interrupt()` on the session agent, `process_registry.kill_all()` (already used by `_mirror_slash_side_effects`), `_clear_pending(session_id)`, and `resolve_gateway_approval(..., "deny", resolve_all=True)`. Returns a structured summary of what was actually stopped. **(B)** Added `setBusy(false)` next to both `setStatusText("Ready")` clear sites in ChatPage (api.getSessionMessages confirm at L1916 AND the gw.connect resume confirm at L2471). Now when the server-side transcript confirms no pending turn, busy clears too. Known unfixed: interrupting an agent that is blocked on a synchronous HTTP call (Codex 300s timeout, OpenAI SDK with no in-flight cancel hook) — `agent.interrupt()` sets `_interrupt_requested=True` + `_set_interrupt(True, tid)` but neither breaks the HTTP wait. That's a separate slice (needs an HTTPX cancel-token path or `httpx.Client.close()` from a separate thread). Filing as P1 todo. | tsc + vite + `python3 -c "import ast; ast.parse(...)"` clean (S34) |
| done | P3 | `/social-media` + `/config` | `social-media-widgets.tsx` empty states + `ModelInfoCard.tsx` capability chips | (1) `social-media-widgets.tsx` had two heavy dashed empty states that survived the cross-page sweeps: L464 was a `rounded-md border border-dashed border-border bg-card px-4 py-10 text-sm` block (videos = []) and L533 was a `rounded-sm border border-dashed border-border bg-card px-2 py-3 text-center text-[0.75rem]` block (rows = []) — both same heavy dashed-card pattern killed in S31/S33/S36. (2) `ModelInfoCard.tsx` capability chips on the model card had palette leaks: Vision used `bg-blue-500/10 text-blue-600 dark:text-blue-400`, Reasoning used `bg-purple-500/10 text-purple-600 dark:text-purple-400`. Each model card rendered 2-3 colored chips fighting for attention — and the colors were raw Tailwind hues, not design tokens. Tools (success token) was the only legit semantic chip; Vision/Reasoning are categorical tags, not status | (1) Both social-media empty states collapsed to the `px-1 py-1 text-xs text-muted-foreground/80` single-line muted convention. (2) Vision + Reasoning chips switched to `bg-muted text-muted-foreground` to match the model_family chip below — categorical tags shouldn't carry color emphasis. Tools (success token) preserved because it IS semantic-status. Net: less visual noise per model card, no more raw Tailwind blue/purple anywhere in `cli/web/src/` | tsc + vite clean (S39) |
| done | P3 | `/admin/templates` + `/leads` thread-drawer | `RealEstateTemplatesPage.tsx` Badge/eyebrow overrides + `real-estate-hub/thread-drawer.tsx` message bubbles | (1) `RealEstateTemplatesPage.tsx` had 5 sites overriding the design system's `Badge`/eyebrow tokens with `font-mono text-[0.6rem] uppercase tracking-wider` (the legacy mono, not the structural `font-mono-ui` variant): 4 Badges with redundant overrides (LaneChannelBadges L169+172, Trial badge L234, Origin badge L307) — Badge already has `font-mono-ui` + `uppercase` + `tracking-[0.06em]` + `text-[0.68rem]` baked in via `components/ui/badge.tsx:7`, so the overrides changed font + tightened text + slightly increased tracking with no design intent. Plus 3 bare spans (MetricCell label L192, version label L225, Proposed timestamp L331) using the same legacy classes — structural enough to keep mono-upper, but should use the canonical `font-mono-ui text-[0.68rem] uppercase tracking-[0.06em]` token set used by `MemoryConstellation.tsx:804` + `LoginCard.tsx:142`. (2) `real-estate-hub/thread-drawer.tsx:65` rendered SMS/email message bubbles with `rounded-2xl` — iMessage-style chunky bubble against a dashboard surface system that's otherwise `rounded-md`/`rounded-lg` | (1) Stripped the 4 Badge overrides — Badges now use the system defaults. Switched the 3 bare span eyebrows to the canonical `font-mono-ui text-[0.68rem] uppercase tracking-[0.06em]` pattern. (2) Thread-drawer message bubbles `rounded-2xl` → `rounded-lg`. Preserves the bubble feel while matching the surface system | tsc + vite clean (S38) |
| done | P1 | `/chat` (cross-stack) | `cli/run_agent.py:Agent.interrupt` | `agent.interrupt()` (run_agent.py:3812) sets `_interrupt_requested=True` and signals tool worker threads via `_set_interrupt(True, tid)`, but the agent's own outbound API request (e.g. openai-codex non-streaming POST with 300s timeout) is a synchronous HTTPX call that does not check either flag until it returns. Result: pressing the Interrupt button (or running `/stop` after S34) does nothing visible for up to 5 minutes when the provider is hung. Same root cause for `Restart Gateway` taking forever to actually shut down — `_drain_active_agents` waits for the in-flight HTTP, can't cancel it. | Used the degraded fallback path: in `interrupt()`, after the existing flag/thread/child signals, look up `self.client._client` (the underlying `httpx.Client`) and call `http_client.close()` from a daemon worker thread (`agent-interrupt-http-close`). Threaded because `httpx.Client.close()` blocks briefly waiting for pool teardown and we don't want to stall the caller (the `/stop` handler from S34 + the gateway interrupt path both expect a fast return). The close raises a connection-closed error in any in-flight outbound request, which falls into the existing retry loop catch at L10800; the interrupt check at L10860 then fires and aborts retries with `interrupted: True` + `final_response="Operation interrupted: handling API error (...)"`. `_ensure_primary_openai_client` already auto-rebuilds closed clients on next use (L5051-5065), so subsequent turns just work. Skipped a full per-request cancel-token layer — would have required wrapping every provider's `chat.completions.create` / `responses.create` call site and is much riskier for the same observable outcome. | `python3 -c "import ast; ast.parse(open('run_agent.py').read())"` clean + tsc + vite clean (S37) |
| done | P2 | `/today` | `real-estate-hub/today/{priority-queue,index,data}.tsx` Priority Queue row clicks | "Needs you now" Priority Queue rows on /today were plain `<Link to={item.to}>` that hard-navigated away — `/leads` for drafts + hot-leads, `/admin` for deal-task + action-run. User flagged this loses the today-page context and forces a route change just to triage a single draft. Drafts and hot-leads already have a working `ThreadDrawer` (sourceId + threadId based) used on `/leads`; just wasn't reachable from `/today` because `ThreadDrawerProvider` was mounted inside the LeadDesk shell only | Extended `UrgentItem` with optional `sourceId?` + `threadId?` and populated them from `draft.sourceId/threadId` and `thread.sourceId/threadId` in `priorityQueue()`. Wrapped `RealEstateTodayPage`'s `HubShell` in `<ThreadDrawerProvider data={data}>` so the today route shares the same drawer. `PriorityRow` now branches: draft / hot-lead with both ids + drawer context available render as a `<button>` that calls `drawer.openThread(sourceId, threadId)` and pops the existing thread drawer in place; admin items (deal-task / action-run) fall back to `<Link to={item.to}>` until they get their own inline detail surface. Thread-drawer JS is now shared via a `thread-drawer-*.js` chunk (~14 kB gzip 3.9 kB) loaded by both /leads and /today, so /today doesn't bloat the today entrypoint | tsc + vite clean (S35) |
| done | P2 | `/leads` + `/today` + global | `_shared/hub-metric.tsx`, `_shared/contact-overview-board.tsx`, `_shared/agent-widgets.tsx`, `components/Toast.tsx` | Four `rounded-2xl` / mono-uppercase violations survived the earlier sweeps because they live in `_shared/` (only loaded by the lazy `RealEstateHubPages` chunk) and the global Toast portal. (1) `HubMetric` tile `rounded-2xl` looks like a marketing card next to the surrounding `rounded-md` border-tinted panels on /leads + /today + /agent-hub. (2) `ContactProfileRow` (contact-overview-board.tsx:10) same `rounded-2xl` issue × every contact row. (3) Contact column empty state (L69) was a `rounded-2xl border border-dashed border-border bg-background/25 px-3 py-6` block — same heavy dashed-card pattern killed in S31 thread-drawer + S33 templates. (4) `agent-widgets.tsx:530` Loop status panel `rounded-2xl` in agent-hub sidebar. (5) `Toast.tsx:27` global toast portal used `font-courier text-xs tracking-wider uppercase` — banned mono-upper on **non-structural copy** (toast bodies are user-facing message strings like "Lead saved", not column headers/timestamps/code). Toast also had no `rounded-*` class at all → sharp corners against rounded surfaces below | (1)(2)(4) `rounded-2xl` → `rounded-md` on the three `_shared/` panels — matches the rest of the dashboard surface system. (3) Contact-column empty state collapsed to the `px-1 py-1 text-xs text-muted-foreground/80` single-line muted convention (drops dashed border, py-6, bg-background/25). (5) Toast.tsx: dropped `font-courier text-xs tracking-wider uppercase` for plain `text-xs`, added `rounded-md` to match the rest of the surface system. Variant colors (success-tinted / destructive-tinted) preserved | tsc + vite clean (S36) |
| done | P0 | desktop shell | `desktop/src/main.js` Electron application menu | macOS routes keyboard accelerators through the application menu before delivering them to the renderer. The Elevate desktop app's menu had Elevate / Navigate / Window / Help submenus but **no Edit submenu** — so Cmd+V, Cmd+C, Cmd+X, Cmd+A, Cmd+Z were never registered as accelerators and the OS swallowed them. Chat composer textarea (ChatPage.tsx:3208) couldn't receive paste events. User reported "can't copy and paste in the chat in the session" | Inserted a conventional Edit submenu between Elevate and Navigate using Electron's built-in roles: undo, redo, separator, cut, copy, paste, pasteAndMatchStyle, delete, selectAll. No keyboard accelerators specified — roles map to platform defaults (Cmd+V on macOS, Ctrl+V elsewhere). Requires desktop app relaunch to pick up since main.js is the Electron main process | node -c (S40 — 71c5ac537) |
| done | P2 | `/today` | `real-estate-hub/admin-task-drawer.tsx` (NEW) + `real-estate-hub/today/{priority-queue,index,data}.tsx` admin row clicks | After S35, draft + hot-lead Priority Queue rows opened the ThreadDrawer inline but admin rows (deal-task with ADMIN tag, action-run) still hard-navigated to `/admin` — user clicked "Run listing-live marketing" expecting a card, got dumped on the kanban page. No admin-task drawer existed in the codebase | Built `AdminTaskDrawerProvider` + `useAdminTaskDrawer` (model mirrors `ThreadDrawerProvider` — portal centered dialog, ESC + backdrop close, body scroll lock). Resolves the target task / run by id from `HubData.dealTasks` / `HubData.actionRuns` rather than re-fetching, so no extra API call to open. Deal-task body shows Deal · Stage · Skill · Description · Status with a "Run with AI" action when `canRunWithAi && skill` (calls `api.runAdminDealTask` with `runNow: true` + refreshes hub data). Action-run body shows Status · Action · Skill · Error with "Approve & run" / "Cancel" actions when `status in (needs_input, blocked, error, failed)` (calls `api.approveAdminActionRun`). Footer has "Open in admin" link as a manual escape hatch. Extended `UrgentItem` with `taskId?` + `runId?`, populated in `urgentAdminTasks()`. Wrapped `RealEstateTodayPage` in `<AdminTaskDrawerProvider>` alongside `ThreadDrawerProvider`. `PriorityRow` resolves a single `inlineHandler` per-kind: draft/hot-lead → thread drawer, deal-task → admin drawer openDealTask, action-run → admin drawer openActionRun. Admin rows now keep the user on /today and let them take the most common action (run with AI / approve) without route change | tsc + vite clean (S41) |
| done | P2 | `/admin` kanban | `real-estate-hub/admin/index.tsx` `AdminPhaseSummary` column-header skill chips | Each of the 10 admin kanban columns (5 stages × 2 sides) rendered a stack of bordered colored skill chips in the column header: stage-entry skills with a Bot icon in `text-primary` (terracotta) with full `border border-current/30 rounded-sm px-1.5 py-0.5`, background skills with a Repeat icon in `text-success` (green) same chip frame, then an approval pill in `text-warning` (amber) with ShieldCheck icon. With 1-3 chips per category × 10 columns the kanban top edge was ~25 colored bordered pills visible at once — every column shouted at the same volume, hard to scan, and the skill names (`listing_launch`, `outreach_run_thread`, etc.) are operator/debug labels not realtor signal. User flagged it as "messy" | Collapsed `AdminPhaseSummary` to a single compact line per column: stage skills as a plain truncated string in `text-foreground/75`, background skills prefixed `bg ·` in `text-muted-foreground`, approval gate as a single `approval` word in `text-warning`. Dot separators (`·` in `text-muted-foreground/45`) between segments. Full skill list lives in the `title` attribute (hover tooltip) so power-users can still get the underlying names. `Moves on {moveSignal}` line preserved below in muted text at `text-[0.66rem]`. Dropped now-unused `Bot` + `Repeat` lucide imports; `ShieldCheck` stays (still used in dialog header + summary banner elsewhere in the file). Added `Fragment` + `type ReactNode` to the React imports for the segments-with-separators render | tsc clean for admin file (ChatPage.tsx errors pre-existing, dirty user work) + vite clean (S42) |
| done | P0 | `/chat` (cross-stack) | `elevate_cli/web_server.py` + `tui_gateway/server.py` + `web/src/lib/api.ts` + `web/src/pages/ChatPage.tsx` chat composer | Chat composer had no way to attach files — user could only send text. No paperclip, no drag-drop, no clipboard image paste. Image-only `vision_analyze` path existed for screenshots but only via slash-command flows, not a generic attachment UI. Non-image files (PDFs, CSVs, transcripts, code snippets) had no path to the agent at all. User requested: "I should be able to drag and drop into it. Like photos, Videos, Pdfs, Txt files, CSV, like everything should be available to send. Screen shots." | Full-stack attachment pipeline. (1) **Web server**: `POST /api/uploads/{session_id}` (`web_server.py`) writes uploads to `<ELEVATE_HOME>/uploads/<session>/<ts>_<name>` with 25 MB per-file cap and filename sanitization. Returns `{path, name, size, media_type}`. (2) **Gateway**: new JSON-RPC methods `file.attach` (routes images into existing `attached_images` queue preserving vision flow; non-images into new `attached_files` queue) and `attachments.clear` (per-path or full clear). `prompt.submit` drains both queues under `history_lock`; new `_enrich_with_attached_files` adds small text/code/CSV/JSON/YAML files (under 200 KB each, 600 KB total budget) as fenced blocks with `[User attached file: name · size · media_type · path: ...]` headers, and references binary/large files by path so the agent can `read_file` them. (3) **API client**: `api.uploadChatAttachment(sessionId, file)` posts multipart form with session header. (4) **ChatPage UI**: `ChatAttachment` state + `attachmentIconFor()` helpers; `AttachmentChipStrip` renders icon/name/size/× chips above textarea inside composer box; `Paperclip` button in `ComposerActionBar` toolbar opens hidden `<input type="file" multiple>`; `onPaste` on textarea converts clipboard file items (screenshots, copied images) via `uploadAttachment`; form-level `onDragEnter/Over/Leave/Drop` with `dragCounterRef` show a dashed accent-tinted "Drop to attach" overlay and route dropped files through `uploadAttachments`. `submitGatewayPrompt` calls `file.attach` per ready attachment before `prompt.submit` and clears the queue after; bails with banner if uploads still in flight | tsc + vite clean (S43), gateway restart required for `file.attach` / upload endpoint to take effect |

| done | P3 | `/admin` | `real-estate-hub/admin/index.tsx` AdminTop25Strip + AdminKanbanBoard empty states | Two heavy `border-dashed border-border bg-card text-center` empty-state cards survived the systematic sweeps (S31, S33, S36). (1) `AdminTop25Strip` at L1602 — no pins rendered a dashed-border centered `px-3 py-4` card with a redundant inner `dev-fallback` sub-chip (already shown on the strip title row above). (2) `AdminKanbanBoard` at L3442 — zero deals rendered a dashed `px-4 py-6 text-center` hero with title + paragraph + a "New deal" button that duplicated the New deal button in the controls bar one row above (L3425). Both still used the dashed-card pattern killed everywhere else | (1) Top25Strip empty state → single muted line `mt-2 px-1 py-1 text-xs text-muted-foreground/80` matching the convention; dropped inner dev-fallback chip. (2) AdminKanbanBoard empty state → single muted line with a pointer to the New deal button above (no duplicate CTA, no dashed card) | tsc + vite clean (S44) |
| done | P1 | `/admin` detail drawer | `real-estate-hub/admin/index.tsx` AdminCardDetailPanel container className | L2542 had a Tailwind class concatenation bug: `bg-cardsm:h-auto` (missing space) — parsed as a single non-existent class, so BOTH `bg-card` (panel background) and `sm:h-auto` (small-screen height override) were dropped. Net effect: detail drawer panel had no background fill on top of the page content; the only thing covering the page behind was the modal backdrop one layer back. Looked transparent in places, especially when the page underneath was bright. Also lost the responsive sm:h-auto so mobile-sized viewports got full-height even when smaller content would fit | Split into `bg-card sm:h-auto` (two classes). No grammar changes — this is purely the typo fix | tsc + vite clean (S45) |
| done | P1 | `/admin` New Deal dialog | `real-estate-hub/admin/index.tsx` NewDealDialog container className | Same `bg-cardsm:h-auto` typo as S45, this time on the NewDealDialog modal at L2932. Same impact: panel had no background fill + sm:h-auto responsive dropped. Full grep sweep for this pattern (`(card|background|muted|primary|destructive|warning|success|foreground|border)(sm|md|lg|xl|hover|focus|...):`) confirms these are the only two instances in the tree | Split into `bg-card sm:h-auto` (two classes) matching S45 | tsc + vite clean (S46) |
| done | P3 | `/admin/templates` | `RealEstateTemplatesPage.tsx` 3 remaining font-mono overrides | After S38 cleaned the 7 most-egregious mono-upper sites, 3 more legacy `font-mono` (not `font-mono-ui`) overrides survived: (1) L371 Rejected/Retired Badge override (`font-mono uppercase tracking-wider`) — Badge already has `font-mono-ui uppercase tracking-[0.06em]` baked in, override changes the font + tightening with no design intent. (2) L455 "Admin · Approval queue" page-header eyebrow + (3) L763 SectionHead label — both structural eyebrows using legacy `font-mono text-[0.62-0.65rem]` instead of the canonical `font-mono-ui text-[0.68rem] tracking-[0.06em]` pattern used everywhere else in S24/S25/S38 | (1) Dropped the Badge className override entirely. (2)+(3) Switched to canonical `font-mono-ui text-[0.68rem] uppercase tracking-[0.06em]` token set | tsc + vite clean (S47) |
| done | P3 | `/sessions` | `SessionsPage.tsx` 4 leftover heavy patterns | After S13 trimmed the expanded SessionRow loading/empty/error states, 4 more heavy patterns survived elsewhere on the same page: (1) SessionMessagesCard (the slide-out detail card) loading rendered a hand-rolled animate-spin spinner in `flex items-center justify-center py-8`; (2)+(3) its error + noMessages used `py-4 text-center` heavy blocks; (4) the page-level filtered.length === 0 zero-state was a full `py-16` hero with a 32px Clock icon, semibold title, and supplementary subtitle | All four converted to the established `px-1 py-1 text-xs text-muted-foreground/80` single-line muted convention. Filtered-empty merges `noSessions` + `startConversation` into a single dash-separated line. Clock import preserved (still used in the CronCard icon map + Cards header at L869) | tsc + vite clean (S48) |
| done | P1 | `/chat` sidebar | `ChatPage.tsx` Artifacts panel | Right-hand activity sidebar's Artifacts list rendered every `ArtifactEntry` write as a separate row, with no height cap. When the agent wrote `config.yaml` 30+ times during a long-running session (e.g. cron task loops, repeated `write_file` calls to the same path), the panel filled the entire sidebar height with 30 identical `config.yaml` rows, pushing the Activity / Progress sections off-screen entirely. User screenshot showed ~32 stacked `config.yaml` entries crowding out everything else. Underlying `addArtifacts` (L1692) keys on `kind:messageId:path:source:title:content[0:120]` so distinct messages writing the same path produce distinct entries by design — fix has to happen at render | New `ArtifactsSection` component groups artifacts by `path || kind:title`, sorts groups by most-recent `createdAt`, and renders one `ArtifactCard` per group with a `count` prop. Card shows `×N` muted suffix when count > 1 (e.g. `config.yaml ×32`) with the count list also reflected in the section header eyebrow (tooltip shows `N unique · M total writes`). List container capped at `max-h-[36vh] overflow-y-auto` so even an un-dedupable list (many distinct files) no longer pushes Activity/Progress off-screen. Underlying `artifacts` state untouched — inline `ChatArtifactShelf` and preview logic keep the full ordered history | tsc + vite clean (S49) |
| done | P3 | `/today` + `/admin` + `/social` | `real-estate-hub/_shared/action-board.tsx` + `_shared/agent-widgets.tsx` + `social-media-widgets.tsx` empty states | 6 leftover heavy empty-state patterns missed by S31/S33/S36/S44/S48 inside the real-estate-hub shared cluster. (1) `action-board.tsx` L61 — ActionBoard zero-actions card body rendered `<div className="py-10 text-sm text-muted-foreground">{empty}</div>` heavy block. (2)+(3) `agent-widgets.tsx` L199 + L269 — RecentSessions + TimedTasks empty bodies same `py-8 text-sm text-muted-foreground` block. (4)+(5) `agent-widgets.tsx` L363 + L430 — AdminDealTasks + AdminActionRuns empty bodies same single-line `py-8 text-sm`. (6) `agent-widgets.tsx` L487 — AgentHandoffsCard's hard-coded `No handoffs yet.` `py-8` block. (7) `social-media-widgets.tsx` L1219 — per-post analytics zero-state rendered a dashed `rounded-sm border border-dashed border-border bg-card px-3 py-4 text-center` card "No metrics returned for this post yet." All 7 sites still gave dead space hero-treatment for what is just an info status | All 7 converted to canonical `<p className="px-1 py-1 text-xs text-muted-foreground/80">…</p>` single-line muted convention matching S31/S33/S36/S44/S48 | tsc + vite clean (S50) |
| done | P3 | `/leads` outreach templates | `RealEstateHubPages.tsx` lane template list zero-state | Last surviving `border-dashed border-border bg-card px-4 py-6 text-center` hero block on the leads page at L3074, inside the OutreachTemplate per-lane list — rendered when `list.length === 0 && showNew !== lane`. Same hero treatment for what is just informational status that the lane will skip drafting | Collapsed to canonical `<p className="px-1 py-1 text-xs text-muted-foreground/80">…</p>` single-line muted. Reworded period-after-sentence to em-dash join so it reads as one phrase | tsc + vite clean (S51) |
| done | P3 | `/agent-hub` | `AgentHubPage.tsx` MiniMetric truncation safety | MiniMetric (used in Harness + Handoffs trios at L769/L937 inside `grid grid-cols-3 gap-2`) had no overflow protection. With three cells across the page at narrow widths and a value like `external_actions_policy: "human_approval_required"` for the Safety metric, the value text would overflow its column and push the layout sideways on small screens — feeding the original "agent hub looks cramped at smaller screens" complaint | Added `min-w-0` to the outer div + `truncate` to both label and value text. Wrapped value with `title={String(value)}` so the full string is recoverable on hover when it truncates. Doesn't change layout at desktop widths where strings fit | tsc + vite clean (S52) |
| done | P0 | `/chat` attachments (cross-stack) | `elevate_cli/web_server.py` upload cap + `cli.py` `_VIDEO_EXTENSIONS` + `tui_gateway/server.py` `_enrich_with_attached_videos` / file.attach / prompt.submit / attachments.clear / session initializers + `web/src/pages/ChatPage.tsx` `ATTACHMENT_MAX_BYTES` | After S43 shipped the generic attachment pipeline, user attached a phone video and hit two problems: (1) frontend + backend both capped per-file at 25 MB, so any real phone clip (60-90s 1080p iPhone capture is typically 80-300 MB) bounced before reaching the agent. User: "Only can send 35mb? for videos? that doesnt help." (2) Videos that did get under cap landed in `attached_files` and `_enrich_with_attached_files` shoved a path-reference header in front of the prompt — agent never actually watched them. `video_analyze_tool` (tools/vision_tools.py:861) already existed (multimodal Gemini-style video_url path, JSON return), it just wasn't wired into the attachment-drain enrichment chain like vision is for images. | (1) Raised `_UPLOAD_MAX_PER_FILE` (web_server.py:1399) 25 MB → 500 MB to cover most phone videos; raised frontend `ATTACHMENT_MAX_BYTES` (ChatPage.tsx:180) to match. (2) Added `_VIDEO_EXTENSIONS` frozenset to cli.py mirroring `_IMAGE_EXTENSIONS` (mp4, webm, mov, avi, mkv, mpeg, mpg, m4v). Added `_enrich_with_attached_videos` to tui_gateway/server.py mirroring `_enrich_with_attached_images`: under 30 MB raw size runs `video_analyze_tool` inline and prepends `[The user attached a video:
{desc}]
[You can re-examine it with video_analyze using video_url: {p}]`; over 30 MB skips inline analysis (provider caps + base64 overhead) and gives size-aware hint-only. Extended `file.attach` (server.py:2677) to route by suffix into a new `attached_videos` queue; `prompt.submit` drains it under `history_lock` alongside images + files and runs the video enrichment between image + file enrichment; `attachments.clear` clears the new queue; all 3 session initializers (L1292, L1349, L1637, L1848) seed `attached_videos: []` + `attached_files: []` for consistency | tsc + vite clean + `python -c 'from cli import _VIDEO_EXTENSIONS; import tui_gateway.server as s'` clean (S53), gateway restart required |
| done | P0 | `/admin` card detail + new-deal modal | `real-estate-hub/admin/index.tsx` AdminCardDetailPanel + NewDealDialog backdrop stacking | Both modals render a sibling `<button>` backdrop (`absolute inset-0 bg-background/80`, AdminCardDetailPanel L2531-2536 / NewDealDialog L2921-2926) and a `<aside>` panel (`relative ...`) inside the same `<div className="fixed inset-0 z-50 flex ...">` parent. Both children are positioned with default `z-index: auto`. Although DOM order *should* put the aside above the backdrop, in practice the X close button area was intercepted by the underlying backdrop button — user reported the X icon on the admin detail panel header was not clickable. Bug surfaces especially with portal mounting + `flex items-stretch` parent. Same risk on NewDealDialog modal | Added explicit `z-0` to the backdrop button and `z-10` to the aside on both modals so the stacking is no longer ambiguous. The X close button and all other in-panel interactive elements now reliably receive clicks instead of bleeding through to the backdrop's onClose handler. Backdrop click-to-close still works via its own onClick | tsc + vite clean (S53) |
| done | P1 | `/admin` card detail panel | `real-estate-hub/admin/index.tsx` AdminCardDetailPanel width + bottom 3-card grid + text scale | After S45 fixed the bg-card typo and S53 fixed the z-index click intercept, the panel was still readability-limited: `sm:max-w-[36rem]` (576px) capped width at all sm+ widths regardless of viewport. The bottom 3-card grid at L2169 (`grid gap-2 sm:grid-cols-3`) gave each card ~175px effective width. The middle Documents card's `item.summary` field is free-form prose (e.g. "MLC package prep notes: verified deal match, recommended BC listing forms, conditional lockbox add-on, and missing approval/field blockers.") and wrapped 14 words into 6+ lines — see Image #53. Body text was uniformly `text-[0.74rem]` (~11.8px), below comfortable reading minimum especially when wrapping happens. /audit scored 17/20 (Good — address layout + typography) | (1) Panel width tier: `sm:max-w-[42rem] md:max-w-[48rem] lg:max-w-[56rem]` — 672/768/896px so panel scales with viewport instead of capping at 576px. (2) Bottom grid switched to asymmetric `sm:grid-cols-[minmax(0,1fr)_minmax(0,2fr)_minmax(0,1fr)]` so the middle Documents card gets double width vs Co-contacts + Prior runs (which hold short structured data). (3) All body-row text bumped `text-[0.74rem]` → `text-xs leading-5` (12px / 20px line-height) for prose legibility; kept the small `text-[0.68rem]` ago-timestamp as accent. (4) Documents row gets `line-clamp-3` + `title={item.summary || item.filePath}` so long summaries bound to 3 lines with hover-revealable full text. Net: prose now reads in 2-3 lines at the new column width instead of 6-7, panel spreads to use available desktop real estate, and small-but-readable structured rows preserve density | tsc + vite clean (S54) |
| done | P2 | `/admin` card detail panel | `real-estate-hub/admin/index.tsx` AdminDealContextSection nested card backgrounds | After S54 widened the panel, the 11 inner cards inside the Transaction-file section (error, no-saved-deal warning, Phase gate, Background automations, Primary contact, Important dates, File details, Co-contacts, Documents, Prior runs, Source actions) all used `bg-card` — the same token as the outer section wrapper at L1978. Result: the inner cards visually melted into the section panel; you could see the borders but the surfaces were the same tone, so the nested hierarchy was illegible. The theme already provides a darker `bg-background` (#1a1b1a) vs `bg-card` (~#212) — inset-well treatment was just unused. Background-automations also has a row of inner chips at L2093 that nest *inside* one of the wells | All 11 nested wells switched `bg-card` → `bg-background` for an inset/recessed feel against the lighter `bg-card` section wrapper. The inner automation chips at L2093 stayed `bg-card`, giving the section a 3-tone depth: section panel (`bg-card`) → wells (`bg-background`, recessed) → chips (`bg-card`, raised inside wells). Wrapper section at L1978 preserved as `bg-card` to mark the section boundary | tsc + vite clean (S55) |
| done | P1 | `/today` + `/tasks` → `/admin` deep link | `_shared/agent-widgets.tsx` AdminDealTasks Open-deal Link + `tasks/index.tsx` DealTaskRow Open-deal Link + `admin/index.tsx` AdminKanbanBoard URL param wiring | The "Open deal" buttons on the Today (AdminDealTasks card) and Tasks (DealTaskRow) pages both rendered `<Link to="/admin">` — clicking just dropped the user on the kanban with no card open. User had to re-find the deal by name in the board, defeating the whole point of the shortcut. User report: "when im in tasks and click open deal it takes me to the admin page but doens't open up the card from the kanban like ti should same thing should happen in todays thing". `AdminDealTask` already carries `dealId` and `side`, so the receiving side just needs to read it and auto-select | (1) Both Link sites now emit `to={\`/admin?deal=${encodeURIComponent(task.dealId)}\`}`. (2) `AdminKanbanBoard` imports `useSearchParams` and adds an effect: when `?deal=` is present and matches a loaded card, it sets `selectedCardId` to the deal and switches `activeSide` to the deal's `listing`/`buyer` swimlane so the panel opens automatically. A `handledDealQueryRef` guard ensures the auto-open only fires once per query value, so closing the panel doesn't immediately re-open it. (3) New `closeDetailPanel` callback clears `selectedCardId` AND strips the `?deal=` param via `setSearchParams({...}, { replace: true })` so the URL stays clean and the deep link doesn't fire again on the next render. Works even if deals are still loading — the effect re-runs when `cards` arrives and finds the match retroactively | tsc + vite clean (S56) |
| done | P1 | `/leads` Open thread drawer | `real-estate-hub/thread-drawer.tsx` modal layout | Thread drawer rendered as a right-edge sliding column: `fixed inset-0 z-50 flex justify-end bg-black/60` with a `flex h-full w-full max-w-[1100px] flex-col border-l border-border bg-background` panel. Result was an off-canvas drawer pinned to the top-right of the viewport that visually clashed with the new centered admin kanban detail modal (S45/S53/S54) — see Image #55, the thread floated under the menu bar with awkward right-edge alignment and a left-only border. User: "open thread should pop up in the middle like the kanban pop ujp.." | Ported the modal wrapper to match `AdminCardDetailPanel` exactly: `fixed inset-0 z-50 flex items-stretch justify-center sm:items-center sm:p-6` outer + sibling `absolute inset-0 z-0 bg-background/80` backdrop button (replaces the click-on-self-then-close handler) + `relative z-10 flex h-full w-full flex-col bg-card sm:h-auto sm:max-h-full sm:w-full sm:max-w-[56rem] sm:rounded-md sm:border sm:border-border lg:max-w-[68rem] xl:max-w-[80rem]` panel (wider than admin since thread is 2-col messages+sidebar). Inner surfaces re-tiered for the new `bg-card` panel context: inbound message bubble L67 `bg-card` → `bg-background`, pending-draft container L255 `bg-card/70` → `bg-background/60`, right rail (Lead score/Notes/Property activity/Send history) L291 `bg-card/30` → `bg-background/40`, attribution chip L376 `bg-card` → `bg-background`, notes list item L433 `bg-card/40` → `bg-background/60`. Modal now mirrors the admin panel's S55 3-tone depth pattern: backdrop dim → `bg-card` panel → `bg-background` wells | tsc + vite clean (S57) |
| done | P1 | `/admin` setup → realtor profile | `real-estate-hub/admin/index.tsx` AdminSetupLaunch Province field | Province in realtor profile (L1077) was a plain `AdminSetupField` text input — user had to type "BC" / "AB" / "ON…" verbatim. No visibility on what local guide coverage exists per province, and no feedback when a province with no docs is entered. User report: "I have to type like BC but that's doesn't do anything. Are the docs set up in the back end flow properly with the download?" Verified backend: `import_exp_agent_centre` runs on every server boot via `local_databases.py:55`, populates `province_reference_pages` / `province_checklists` / `province_forms` from `~/.elevate/knowledge/skyleigh/admin/exp-agent-centre`. Operational DB confirmed: 11 provinces with 4-26 reference pages each, BC has the full transaction guide (17 checklists + 34 forms). The download IS wired up — the gap is purely UX: user couldn't see what they were getting | Replaced the single Province text input with a Country dropdown ("Canada", disabled since only Canada is currently supported but the slot exists for expansion) + a Province dropdown populated from the existing `CANADIAN_PROVINCES` constant (also used by NewDealDialog). Each province option suffixes " — full guide" or " — reference" based on coverage, matching the NewDealDialog pattern. Below the select, the existing S55-style chip row surfaces `full guide`/`reference` + `referencePages` + `forms` + `checklists` counts. `AdminSetupLaunch` now fetches province coverage on mount via `api.getAdminProvinceGuides()`. If user picks a province with zero coverage (currently NU + NT — Nunavut and Northwest Territories aren't in the imported set), an inline `text-[11px] text-muted-foreground` line warns about manual fallback. Province codes are normalized to uppercase on select to match the storage format the rest of the system expects | tsc + vite clean (S58) |
| done | P1 | `/admin` setup province coverage data | `elevate_cli/data/province_guides.py` import generalization + scrape sync | After S58 wired the Province dropdown to coverage chips, the chips showed thin numbers per province: AB/MB/NB/NL/ON/PEI/QC/SK/YK all at 4-5 reference pages, 0 checklists, 0 forms. Only BC had the full transaction guide (17 checklists + 34 forms). User: "we did this already and got all provinces — why are they not wired?" Investigation found the deeper scrape at `~/skyleigh-tools/knowledge/exp-agent-centre/` with per-province transaction guides for AB (58 files), BC (73), MB (40), NB (33), NL (33), ON (118), QC (33), SK (33), YT (56) — 552 files vs the 74-file subset that was synced into `~/.elevate/knowledge/skyleigh/admin/exp-agent-centre/`. `import_exp_agent_centre` (L494) was hardcoded to `base / "transaction-guide-bc"` so only BC's transaction guide ever got ingested, even when the others existed on disk | (1) Generalized the import: replaced the BC-only block with `for guide_root in sorted(base.glob("transaction-guide-*"))` loop. The folder suffix maps to a province code via the existing `_PREFIX_TO_PROVINCE` dict (e.g. `transaction-guide-ab` → AB, `transaction-guide-yt` → YK). Each guide root contributes checklists from `*.md`, best-practice pages from `best-practices-topics/*.md`, and forms from `forms/inventory.json` — same shape as the original BC block, just driven by directory discovery. (2) Added `"yt": "YK"` to `_PREFIX_TO_PROVINCE` because the scrape used `yt` (Yukon Territory) where the rest of the system uses `YK` (Yukon). (3) Kept the BC-specific `_DEFAULT_CONDITIONAL_DOCS` seed gated on `"BC" in target_set` so per-province imports don't redundantly re-seed them. (4) rsync'd the 8 missing transaction-guide folders from the skyleigh-tools mirror into the elevate root so the boot-time importer picks them up. Re-ran `import_exp_agent_centre` against the live operational DB: row counts jumped from 74 pages / 17 checklists / 34 forms to **161 pages / 230 checklists / 137 forms** spanning 11 provinces (AB 9/24/29, BC 26/17/34, MB 18/19/8, NB 18/19/1, NL 18/19/1, NS 5/0/0, ON 17/70/35, PEI 5/0/0, QC 18/19/1, SK 18/19/1, YK 9/24/27). Second-run idempotency verified — upserts keyed on (province, slug) so re-running on boot doesn't dup rows. AdminSetupLaunch coverage chips now reflect per-province depth on next dropdown selection without further UI changes | tsc + vite clean (S59) |
| done | P3 | `/admin` setup → realtor profile | `real-estate-hub/admin/index.tsx` AdminSetupLaunch Country dropdown | After S58 introduced the Country + Province dropdown pair, the Country slot held a disabled `<select>` with `"Canada"` as the only option. On the 3-col `lg:grid-cols-3` Realtor profile grid that disabled cell consumed a full column slot and signaled "this might change" when in fact the system only supports CA. Disabled-select-with-one-option is a known anti-pattern (wastes real estate, lies about interactivity) | Removed the dedicated Country cell entirely and inlined a small `CA · Canada` right-aligned mono chip into the Province field's label row. Country stays visible (we still need users to know we're scoped to CA) but it doesn't pretend to be interactive and doesn't steal a grid column. Net: Realtor profile gains a usable column slot, the Country/Province pair reads as a single locale field instead of two redundant controls | tsc + vite clean (S60) |
| done | P2 | `/admin` setup → providers | `real-estate-hub/admin/index.tsx` AdminSetupField + PROVIDER_SUGGESTIONS | The Providers grid had 13 freeform text inputs (Email, Calendar, Drive, CRM, MLS, Forms, Signing, Compliance, Showing, Photo processing, FINTRAC) — placeholders already listed canonical values ("Lofty, kvCORE, BoldTrail...", "SkySlope / Lone Wolf", "ShowingTime / BrokerBay") because the system expects users to type one of a known short list. Without suggestions, users type variations ("SkySlope" vs "skyslope" vs "Sky Slope") and downstream matching by provider name (admin_setup.py:544 wires CRM/signing into provider memory strings) silently misses | `AdminSetupField` now takes optional `suggestions` + `listId` props that render an HTML5 `<datalist>` next to the `<input>`. Native browser combobox: typing filters suggestions, picking one auto-fills, and freeform input still works for outliers. Added 11 canonical option lists in `PROVIDER_SUGGESTIONS` (e.g. CRM: Lofty / kvCORE / BoldTrail / Follow Up Boss / Sierra Interactive / Chime / HubSpot; Signing: DigiSign / DocuSign / Authentisign / Dotloop / PandaDoc; Compliance: SkySlope / Lone Wolf / Dotloop / BrokerWolf; Showing: ShowingTime / BrokerBay / Aligned Showings / ShowingSmart). Wired all 11 Providers grid fields. No state, no Radix, no controlled-component overhead — datalist is the right tool for type-and-pick freeform fields | tsc + vite clean (S61) |
| done | P1 | `/admin` setup + NewDealDialog | `real-estate-hub/admin/index.tsx` AdminSetupLaunch + NewDealDialog province lock | After S58/S59 made the dropdown show every Canadian province with "full guide" / "reference" suffixes, the picker stayed in choose-mode forever — every time the user opened `/admin` or NewDealDialog they re-saw all 13 provinces with quality annotations even though they'd already committed to (say) BC. User: "once smeone picks it it should not show the other ones, only stick with the province." That's right: the multi-province dropdown is a decision-time affordance, not a steady-state one. Steady state should be locked to the saved province with a low-friction escape hatch | Added `PROVINCE_LABEL_BY_CODE` lookup near `CANADIAN_PROVINCES`. **AdminSetupLaunch**: when `setup.profile.province` has a saved value AND user hasn't clicked "Change", render a read-only `bg-muted/40` chip with the province name + `saved` mono-tag instead of the full dropdown. "Change" link top-right of the label row flips local `provinceUnlocked` state and brings the dropdown back. Re-locks automatically after each save. **NewDealDialog**: pre-fills province from `getAdminJurisdiction()` (already did) but now stores it in `setupProvince` state. While `setupProvince` is set and `provinceOverride` is false, the dropdown is replaced with a `bg-muted/40` "from setup" chip. "Change for this deal" link opens the dropdown for that one deal; "Reset to setup (CODE)" link snaps back. Coverage chips still render below for the active selection. Net: 99% of the time users see only their own province; cross-province edge cases (rare second-jurisdiction deal) still possible behind an explicit two-click affordance | tsc + vite clean (S62) |
| done | P1 | adaptive kanban backend | `elevate_cli/data/province_guides.py` province_stage_documents mapper | The 10-stage admin kanban spine is identical for every deal, but the *documents required inside each stage* differ per province (BC: MLC/PDS/BAEC/CPS; ON: numbered Forms 100/200/121/124; AB: ESRA/EBRA/RPS/dower-consent; MB: form-e + condo PDS). Today the UI hardcodes a BC-flavored `ADMIN_STAGE_CHECKLISTS` (S4 mentions "evalue_bc", S7 says "Subject removal", S8 says "Conveyancer package") so an ON or AB user sees BC vocabulary even when their own province's full forms inventory is sitting in `province_forms`. User: "the kanban board should be based on the same flow but with different steps in those phases based on the documents." Decision: don't parse `province_checklists` markdown bodies (fragile against scrape variations — BC is clean but AB tables are flattened). Drive the adaptive layer off the structured `province_forms` (code/name/category) instead. Checklists stay as reference material the UI can link to | New `province_stage_documents(conn, province, side, conditions)` function maps each province's forms inventory to the 10 admin stages via ordered regex/keyword rules. Province-specific rules (BC: MLC→S1, PDS→S2, CPS→S6, BAEC→S1 buyer; ON: Form 100/101/500→S6, Form 121/122/123/124→S7, Form 200/240/241/242→S1, Form 218/371→S0; AB: ESRA→S1, RPS/agri/dower→S6, EBRA→S1 buyer, notice-satisfy→S7, addendum→S7; MB: MLS-listing-contract→S1, marketing-release→S4, direction-offers→S6) tried first, then a generic fallback layer (purchase agreement→S6, amendment/waiver→S7, fintrac/identification→S1, closing/conveyance→S8) covers the long tail (NB/NL/PEI/QC/SK all have just `create-deal-sheet`). Category-only rules cover unclassified BC `category` fields (Listing/Buyer/Disclosure/Rental/Additional). Side filtering: rules tagged `listing`/`buyer` are skipped for the other side and returned as `otherSide` (informational, not surfaced as gaps); rules with side=None apply to both. `conditional_docs` overlay layered on top (e.g. `tenanted=true` → tenancy docs S2, `property_subtype=strata` → strata docs S6, `multiple_offers=true` → offer matrix S6, `poa_signing=true` → POA authority S3). Verified coverage on live DB: BC 29/34 mapped + 5 buyer-side filtered (0 unmapped), ON **35/35** mapped, AB 28/29 mapped, MB 8/8 mapped, SK 1/1 (just deal-sheet). Helper introduces no new tables, no schema changes, no callers yet — pure backend foundation for the upcoming API + UI slices. Returns shape: `{province, side, stages: {0:[…], 1:[…], …}, unmapped, otherSide, coverage}` where each item has `{code, name, source: "form"\|"conditional", side, category, sourcePath, condition?}` | `python3 -c "import ast; ast.parse(open('elevate_cli/data/province_guides.py').read())"` clean + live-DB smoke run across BC/ON/AB/MB/SK both sides (S63) |
| done | P1 | adaptive kanban context payload | `elevate_cli/data/deals.py` get_deal_context + `web/src/lib/api-types.ts` DealContext | The S63 mapper produces per-stage province-specific documents, but it had no callers — the AdminCardDetailPanel still relied on the hardcoded BC-flavored `ADMIN_STAGE_CHECKLISTS`. Wiring options were (a) a new `GET /api/admin/deals/{id}/stage-documents` endpoint or (b) extending the existing `get_deal_context` payload that the panel already consumes. Chose (b): no new route, no extra round-trip, no client query state — the mapper runs server-side once when the panel opens. The existing context endpoint already pulls `province_guide_summary` + `condition_docs_for_conditions`, so adding the new mapper next to them is the minimum surface change | (1) Added `stage_documents` field to `get_deal_context` (deals.py) via `province_stage_documents`. Conditions input merges the named-field conditions (signing_authority/fintrac_form_type/etc) with the deal's `checklist` boolean toggles (tenanted/strata/lockbox/multiple_offers/poa_signing/...) and `propertySubtype` — booleans normalize to "true"/"false" strings so they match the `conditional_docs` `field_value` column. (2) Province normalization: deal rows store province as either a clean code ("BC") or a legacy country-tuple ("ca.bc"). Added a one-liner `raw_province.split(".", 1)[-1]` strip + `normalize_province_code` (already exists) so every downstream call — `condition_docs_for_conditions`, `province_guide_summary`, `province_agent_memory`, `province_stage_documents` — sees the canonical short code. Live verification: a deal with `province="ca.bc"` now resolves to 29 mapped forms across stages 0/1/2/6/7 (was 0 before the normalize step). (3) Exported `province_stage_documents` from `elevate_cli/data/__init__.py` (both the import block and `__all__`). (4) TypeScript: added `ProvinceStageDocumentItem` + `ProvinceStageDocuments` interfaces and a `stageDocuments?: ProvinceStageDocuments` field on `DealContext`. Mirrors the Python shape — `stages: Record<string, ProvinceStageDocumentItem[]>` keyed by stage number string so the UI can index directly into `dealContext.stageDocuments?.stages[String(stageIndex)]` | tsc clean + vite build clean (2.05s); live-DB smoke run shows 5/5 sampled deals return 29 mapped items each (S64) |
| done | P1 | adaptive kanban UI surface | `real-estate-hub/admin/index.tsx` AdminCardStageSection | S63 produced the mapper, S64 surfaced its output on `DealContext.stageDocuments`. But the AdminCardDetailPanel's stage rows still rendered only the hardcoded BC-flavored `ADMIN_STAGE_CHECKLISTS` (S4 "evalue_bc", S7 "Subject removal", S8 "Conveyancer package"). An ON, AB, or MB user opening the panel saw zero province-specific document steps even though the payload now carried them. The operational checklist itself stays — it's the realtor's task list, not the regulator's form list — but the province documents need their own dedicated subsection inside each expanded stage row | Added a `documents?: ProvinceStageDocumentItem[]` prop on `AdminCardStageSection`. Inside the `{expanded && ...}` block, after the existing operational checklist `<ul>`, render a separator-divided `Province documents · N` block when `documents.length > 0`. Each row uses `FileText` mini-icon + `code · name` with the code emphasized (`font-medium text-foreground`) and the name muted. Conditional docs (overlaid by S63's `conditional_docs` rules) get a small `font-mono-ui text-warning` `if field=value` tag so the user knows *why* an extra form appeared (e.g. `if tenanted=true`, `if property_subtype=strata`). Threaded the prop at the render site: `documents={dealContext?.stageDocuments?.stages[String(stage)] ?? []}` — falls back to empty array when context still loading, when deal is unsaved (no persisted id → no context fetched), or when the province has no forms mapped to that stage. Empty array means the block self-hides via the `documents.length > 0` guard, so stages with only operational steps render exactly as before — no visual regression for any province. Imported `ProvinceStageDocumentItem` from `@/lib/api` next to existing `DealContext` type. Code uses the already-imported `FileText` lucide icon — no new dependencies | tsc -b exit 0; vite build clean in 1.88s; RealEstateHubPages chunk 70.15 kB / gzip 18.59 kB (was 69.x — only ~1 kB added for the documents block) (S65) |
| done | P2 | adaptive kanban operational labels | `real-estate-hub/admin/index.tsx` ADMIN_STAGE_CHECKLISTS | The hardcoded operational checklist that sits above S65's new Province documents block still spoke pure BC: "eValue BC age verified" (S4 listing), "Subject removal form sent" / "BIR + PDS received" (S7 listing), "Conveyancer package sent" (S8 listing), "CPS input checklist + offer strategy" / "Doc list (CPS, addenda, disclosures, deposit receipt)" (buyer S3-S4), "Strata review (if applicable)" (buyer S5), "All subjects removed" (buyer S7). An ON or AB realtor opening the panel saw BC vocabulary even when their province's actual form codes (Forms 121/122 / ESRA / EBRA / etc) were now showing correctly below. Operational steps don't need to be province-specific — the *act* (verify photo age, send condition waiver, request disclosure docs, deliver package to lawyer) is universal even though the form codes differ. So the right fix is province-agnostic labels, not per-province operational templates | Eight label-only edits (no id changes, so persisted `completedByStage` boolean state still applies): listing S4 "eValue BC age verified" → "Property valuation age verified"; listing S7 "Subject removal form sent" → "Condition removal / waiver sent"; listing S7 "BIR + PDS received" → "Property disclosure docs received"; listing S7 stage-complete "Subject removal verified" → "Conditions removed / waived"; listing S8 "Conveyancer package sent" → "Lawyer / conveyancer package sent"; buyer S3 "CPS input checklist + offer strategy" → "Offer document checklist + strategy"; buyer S4 "Doc list (CPS, addenda, disclosures, deposit receipt)" → "Doc list (offer, addenda, disclosures, deposit receipt)"; buyer S5 "Strata review (if applicable)" → "Strata / condo review (if applicable)"; buyer S7 "All subjects removed" → "All conditions removed / waived". Province-specific form *codes* (MLC, BAEC, ESRA, Form 121, etc) live below in the new S65 Province documents block where they belong — so a BC user still sees "MLC" + "BAEC" alongside the generic "Condition removal / waiver sent" label, and an ON user sees "Form 121" / "Form 122" instead, all driven by data | tsc clean + vite build clean in 1.91s (S66) |
| done | P2 | adaptive kanban stage labels | `real-estate-hub/admin/index.tsx` ADMIN_STAGE_DEFINITIONS + dateRows | The kanban column labels themselves carried BC-specific vocabulary: S7 was named "Subjects" (stageLabel + listing title "Subject Removal" + buyer title "Subjects Off" + subtitle "Subjects + lawyer package"), S4 buyer subtitle said "Comps + CPS", S6 buyer subtitle said "Inspection + strata", S8 listing subtitle said "Conveyance + possession", and the date table in the deal panel had a "Subjects" row label. These are the most visible UI tokens — they show on every column header and in every deal panel regardless of province | Renamed S7 across the board: stageLabel "Subjects" → "Conditions"; listing title "Subject Removal" → "Condition Removal"; buyer title "Subjects Off" → "Conditions Removed"; subtitle "Subjects + lawyer package" → "Conditions + lawyer package". S4 buyer subtitle "Comps + CPS" → "Comps + offer paperwork". S6 buyer subtitle "Inspection + strata" → "Inspection + property review". S8 listing subtitle "Conveyance + possession" → "Lawyer / conveyance + possession". Date table row label "Subjects" → "Conditions" (the underlying field `deal.subjectRemovalDate` stays — that's a DB column we don't migrate over a cosmetic label change). The Python `admin_deal_flow.py` (used only by the v1 CLI fallback flow definitions, not by the UI) still says "Subjects + lawyer package" — leaving that alone since it's a CLI-only artifact; the UI is the source of truth for what users see | tsc clean + vite build clean in 1.85s; ChatPage/admin chunk sizes unchanged (S67) |
