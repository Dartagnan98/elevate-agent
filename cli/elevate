#!/usr/bin/env python3
"""
RAgent Agent CLI launcher.

This wrapper should behave like the installed `ragent` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from ragent_cli.main import main
    main()
